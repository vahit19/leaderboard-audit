__pycache__/
*.py[cod]
.venv/
venv/
.env
*.egg-info/
.pytest_cache/
.DS_Store

# run artefacts and caches: regenerate with `python demo.py`
run_demo/
run_live/
results_live/
.cache/
.cache_demo/
*.jsonl.bak
run_smoke/
run_smoke2/

# published run tables are tracked; their raw logs and caches are not
run_gsm8k_numeric/results.jsonl
run_gsm8k_judge/results.jsonl
run_gsm8k_*/errors.jsonl
run_j_*/results.jsonl
run_j_*/errors.jsonl

# Personal material kept in the working copy, never published.
# These sit beside the code and are not ignored by any rule above, so a
# plain `git add -A` would stage and publish them.
my_cv/
paper/
*.pdf
*.docx
